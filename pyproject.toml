[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "marketspike"
version = "1.0.0"
requires-python = ">=3.8"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
